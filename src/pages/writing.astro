---
import BaseLayout from '../layouts/BaseLayout.astro';

const imageExample = `{
  type: 'image',
  src: '/gallery/photo-2026-05-04.jpg',
  title: '照片标题',
  description: '这里写照片描述。'
}`;

const videoExample = `{
  type: 'video',
  src: '/gallery/video-2026-05-04.mp4',
  title: '视频标题',
  description: '这里写视频描述。'
}`;
---

<BaseLayout title="内容发布指南" description="如何新增博客、照片墙内容和附件。">
  <section class="site-shell page">
    <header class="page-header">
      <span class="eyebrow">Guide</span>
      <h1>内容发布指南</h1>
      <p>这里说明三件事：如何添加博客文章、如何更新照片墙、如何给文章或页面添加附件。</p>
    </header>

    <div class="article-body writing-guide">
      <h2>一、添加博客文章</h2>
      <p>文章统一放在 <code>src/content/blog/</code> 目录下，每篇文章是一个 Markdown 文件。</p>

      <h3>推荐方式：本地脚本创建</h3>
      <p>在项目根目录运行：</p>
      <pre><code>npm run new:post</code></pre>
      <p>脚本会依次询问文章标题、摘要、分类和标签，然后自动在 <code>src/content/blog/</code> 下生成文章文件。</p>

      <h3>手工方式：在 GitHub 网页添加</h3>
      <ol>
        <li>打开仓库 <a href="https://github.com/hututu1226/blog">hututu1226/blog</a>。</li>
        <li>进入 <code>src/content/blog</code> 目录。</li>
        <li>复制 <code>_template.md</code> 的内容。</li>
        <li>点击 <code>Add file</code>，选择 <code>Create new file</code>。</li>
        <li>文件名建议写成 <code>2026-05-04-my-note.md</code>，尽量使用英文、数字和短横线。</li>
        <li>把 <code>draft</code> 改成 <code>false</code>，填写正文后提交。</li>
      </ol>

      <h3>文章模板</h3>
      <pre><code>---
title: 文章标题
description: 文章摘要，会展示在列表页
pubDate: 2026-05-04
category: 知识管理
tags: [博客, 记录]
draft: false
---

这里写正文。</code></pre>

      <p>当前分类固定为：<code>知识管理</code>、<code>项目</code>、<code>阅读</code>、<code>生活杂谈</code>。</p>

      <h2>二、添加照片或视频</h2>
      <p>照片墙页面在 <code>src/pages/gallery.astro</code>，媒体文件建议放到 <code>public/gallery/</code>。</p>

      <h3>推荐方式：本地脚本导入</h3>
      <p>在项目根目录运行：</p>
      <pre><code>npm run new:gallery</code></pre>
      <p>脚本会询问图片/视频路径、标题和描述，然后自动复制文件到 <code>public/gallery/</code>，并更新 <code>src/data/gallery.json</code>。</p>

      <h3>手工方式：自己修改数据</h3>
      <ol>
        <li>把图片或视频上传到 <code>public/gallery/</code>。</li>
        <li>打开 <code>src/data/gallery.json</code>。</li>
        <li>在数组里新增一项。</li>
      </ol>

      <h3>图片示例</h3>
      <pre><code>{imageExample}</code></pre>

      <h3>视频示例</h3>
      <pre><code>{videoExample}</code></pre>

      <p>建议图片控制在 1MB 左右，视频尽量压缩后再上传，避免页面加载太慢。</p>

      <h2>三、添加附件</h2>
      <p>附件适合放 PDF、Word、PPT、压缩包、数据文件等。建议统一放到 <code>public/attachments/</code>。</p>

      <ol>
        <li>把附件上传到 <code>public/attachments/</code>。</li>
        <li>在文章 Markdown 中用普通链接引用。</li>
      </ol>

      <h3>附件链接示例</h3>
      <pre><code>[下载项目说明 PDF](/attachments/project-note.pdf)

[下载阅读笔记压缩包](/attachments/reading-notes.zip)</code></pre>

      <p>如果后续网站绑定到 <code>hututu1226.top</code>，上面的链接会变成：</p>

      <pre><code>https://hututu1226.top/attachments/project-note.pdf</code></pre>

      <h2>四、发布流程</h2>
      <p>无论是新增文章、照片还是附件，提交到 GitHub 后，EdgeOne Pages 会重新构建并发布网站。</p>

      <pre><code>修改内容
  -> 提交到 GitHub main 分支
  -> EdgeOne Pages 自动构建
  -> 网站更新</code></pre>
    </div>
  </section>
</BaseLayout>
